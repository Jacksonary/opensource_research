package com.hhu.zcy.str;

/**@formatter:off 
 * 给定一个字符类型的数组 chas[]，chas 右半区全是空字符，左半区不含有空字符。现在想
 * 将左半区中所有的空格字符替换成"%20"，假设 chas 右半区足够大，可以满足替换所需要的空
 * 间，请完成替换函数。
 * 【举例】
 * 如果把 chas 的左半区看作字符串，为"a b c"，假设 chas 的右半区足够大。替换后，chas
 * 的左半区为"a%20b%20%20c"
 * @formatter:on 
 * @author jacks
 * @date 2022/7/6
 */
public class StrAdjust {
    private char[] replaceBlank(char[] chas) {
        return null;
    }
}
